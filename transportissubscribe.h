#ifndef TRANSPORTISSUBSCRIBE_H
#define TRANSPORTISSUBSCRIBE_H

#include <QObject>
/*
class TransportIsSubscribe : public QObject
{
  Q_OBJECT
public:
  explicit TransportIsSubscribe(QObject *parent = 0);

  void readPathToSubscribes(QString);

  void runTransportMail();


signals:

public slots:

private:
  QString path_to_subscribes;
  QList <int> active_subscribe;
  QStringList arguments;
  QString message;
  QString out_error;
  void startTransport();
  void setMessage(int, int);
  void transportMail();
  void setResult(int, int);
  void readActiveSubscribe();
};
*/
#endif // TRANSPORTISSUBSCRIBE_H
